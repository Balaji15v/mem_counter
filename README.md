# mem_counter
This is the circuit in which the data read/write operation is made and then using load signal from a specified address the data is loaded for the counter circuit in which both the up and down counter happens. We can also replace BCD counters, ripple counters and all, but the count starts from the data in or from the counters initial condition
